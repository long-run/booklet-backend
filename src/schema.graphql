type Query {
}

type Mutation {
}

type User {
  u_idx: String!
  u_id: String!
}

type BookFeed {
  uniq_idx: String!
  book: Book!
  rating: Number!
  status: FeedStatus!
  background_theme: String!
  note: String!
  reg_date: String!
  modified_date: String!
}

type Post {
  user: User!
  bookfeed: BookFeed!
  like_count: Number!
  liked: Boolean!
}

type Book {
  title: String!
  isbn: String!
  author: String!
  book_cover: String!
}

enum FeedStatus {
  currently_reading, read, want_to_read
}